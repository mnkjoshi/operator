# Operator Architecture Overview

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                          │
│                      (Browser @ :3000)                          │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP/WebSocket
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                      FRONTEND (React)                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                     React Router                          │  │
│  │          (Focus Management + Helmet)                     │  │
│  └────────────────────┬─────────────────────────────────────┘  │
│                       │                                         │
│  ┌────────────────────▼─────────────────────────────────────┐  │
│  │                 Layout Component                          │  │
│  │  ┌──────────────────┐      ┌──────────────────────────┐  │  │
│  │  │  Canvas (70%)    │      │  Agent Panel (30%)      │  │  │
│  │  │                  │      │  ┌────────────────────┐  │  │  │
│  │  │  • Simplified    │      │  │ MicrophoneButton  │  │  │  │
│  │  │    Content       │      │  └────────────────────┘  │  │  │
│  │  │  • High Contrast │      │  ┌────────────────────┐  │  │  │
│  │  │  • Semantic HTML │      │  │    Chat Log       │  │  │  │
│  │  │  • ARIA Labels   │      │  │  (ARIA Live)      │  │  │  │
│  │  │                  │      │  └────────────────────┘  │  │  │
│  │  │                  │      │  ┌────────────────────┐  │  │  │
│  │  │                  │      │  │  Action Buttons   │  │  │  │
│  │  │                  │      │  │  • Read           │  │  │  │
│  │  │                  │      │  │  • Simplify       │  │  │  │
│  │  │                  │      │  │  • Explain        │  │  │  │
│  │  │                  │      │  └────────────────────┘  │  │  │
│  │  └──────────────────┘      └──────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Styling: Clarity Design System (Tailwind + Radix UI)          │
│  Linting: ESLint + jsx-a11y (strict)                          │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           │ REST API Calls
                           │
┌──────────────────────────▼──────────────────────────────────────┐
│                    BACKEND (FastAPI)                            │
│                     @ localhost:8000                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                   FastAPI Router                          │  │
│  │                  (CORS Middleware)                        │  │
│  └────┬─────────────────┬────────────────────┬───────────────┘  │
│       │                 │                    │                  │
│  ┌────▼─────┐     ┌─────▼──────┐     ┌──────▼─────┐           │
│  │  Agent   │     │   Speech   │     │  Simplify  │           │
│  │  Router  │     │   Router   │     │   Router   │           │
│  │          │     │            │     │            │           │
│  │ /chat    │     │ /stt       │     │ /          │           │
│  │ /action  │     │ /tts       │     │ /remove-   │           │
│  │          │     │ /voices    │     │  noise     │           │
│  └────┬─────┘     └─────┬──────┘     └──────┬─────┘           │
│       │                 │                    │                  │
└───────┼─────────────────┼────────────────────┼──────────────────┘
        │                 │                    │
        │                 │                    │
┌───────▼─────────────────▼────────────────────▼──────────────────┐
│                    EXTERNAL AI SERVICES                          │
│  ┌────────────┐     ┌────────────┐     ┌────────────────────┐  │
│  │   Gemini   │     │  Whisper   │     │   ElevenLabs       │  │
│  │            │     │            │     │                    │  │
│  │ • Chat     │     │ • Speech   │     │ • Text-to-Speech   │  │
│  │ • Simplify │     │   to Text  │     │ • Natural Voices   │  │
│  │ • Explain  │     │            │     │                    │  │
│  └────────────┘     └────────────┘     └────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow Examples

### Example 1: User Speaks Request

```
1. User clicks Microphone Button
   │
   ├─> Frontend: Start audio recording (Web Audio API)
   │
2. User speaks: "Read this to me"
   │
   ├─> Frontend: Capture audio stream
   │
3. Frontend sends audio to Backend
   │   POST /api/speech/stt
   │
   ├─> Backend: Forward to OpenAI Whisper
   │
4. Whisper transcribes: "Read this to me"
   │
   ├─> Backend: Returns text transcript
   │
5. Frontend displays in Chat Log
   │
6. Frontend sends to Agent
   │   POST /api/agent/action
   │   action: "read", content: [page content]
   │
   ├─> Backend: Gemini processes request
   │
7. Backend generates speech
   │   POST /api/speech/tts
   │
   ├─> ElevenLabs: Converts text to audio
   │
8. Frontend plays audio to user
   └─> User hears content read aloud
```

### Example 2: Simplify Content

```
1. User clicks "Make it simpler" button
   │
   ├─> Frontend: Get current page content
   │
2. Frontend sends request
   │   POST /api/simplify/
   │   { content: "...", level: "simple" }
   │
   ├─> Backend: Gemini simplifies text
   │
3. Backend returns simplified content
   │
   ├─> Frontend: Display in Canvas
   │
4. Screen reader announces change
   └─> User hears simplified version
```

## 🎨 Clarity Design System Flow

```
┌──────────────────────────────────────────────────────────────┐
│              Clarity Design System                           │
│                                                              │
│  Colors                Typography           Spacing         │
│  ├─ Dark: #121212      ├─ Atkinson         ├─ Touch: 48px  │
│  ├─ Light: #FAFAFA     │   Hyperlegible    ├─ Base: 18px   │
│  └─ Focus: #FFB000     └─ Line: 1.5x       └─ Scale: 1.5x  │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              Tailwind CSS Configuration               │ │
│  │         (tailwind.config.js + index.css)              │ │
│  └────────────────────────────────────────────────────────┘ │
│                           │                                  │
│                           ▼                                  │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              Component Implementation                  │ │
│  │     (All components use design tokens)                │ │
│  └────────────────────────────────────────────────────────┘ │
│                           │                                  │
│                           ▼                                  │
│  ┌────────────────────────────────────────────────────────┐ │
│  │           ESLint jsx-a11y Validation                  │ │
│  │      (Enforces WCAG 2.2 AAA standards)                │ │
│  └────────────────────────────────────────────────────────┘ │
│                           │                                  │
│                           ▼                                  │
│  ┌────────────────────────────────────────────────────────┐ │
│  │            Storybook Testing                          │ │
│  │    (Visual accessibility validation)                  │ │
│  └────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

## 🧩 Component Hierarchy

```
App (React Router + Helmet Provider)
│
└─── Layout
     │
     ├─── Skip-to-Content Link
     │
     ├─── Canvas (main, 70%)
     │    │
     │    └─── Outlet (React Router)
     │         │
     │         └─── Home Page
     │              ├─── Heading (h1) [focus target]
     │              ├─── Mission Section (h2)
     │              ├─── Features List (ul)
     │              └─── Getting Started (h2)
     │
     └─── Agent Panel (aside, 30%)
          │
          ├─── Heading (h2)
          │
          ├─── Microphone Button
          │    ├─── SVG Icon
          │    ├─── aria-pressed state
          │    └─── 48x48px touch target
          │
          ├─── Chat Log
          │    ├─── role="log"
          │    ├─── aria-live="polite"
          │    └─── Message bubbles
          │
          ├─── Action Buttons
          │    ├─── Read button (🔊)
          │    ├─── Simplify button (✨)
          │    └─── Explain button (💡)
          │
          └─── Status Indicator
               ├─── role="status"
               └─── aria-live="polite"
```

## 🔐 Security & Configuration Flow

```
┌──────────────────────────────────────────────────────────────┐
│                  Environment Variables                        │
│                    (backend/.env)                            │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  GEMINI_API_KEY=xxx                                    │  │
│  │  OPENAI_API_KEY=xxx                                    │  │
│  │  ELEVENLABS_API_KEY=xxx                                │  │
│  │  CORS_ORIGINS=http://localhost:3000                    │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────┬───────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│               FastAPI Application Init                       │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  1. Load environment variables (python-dotenv)        │  │
│  │  2. Configure CORS middleware                         │  │
│  │  3. Initialize AI clients (Gemini, OpenAI, ElevenLabs)│  │
│  │  4. Register routers                                  │  │
│  └────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────┘
```

## 📦 Build & Deploy Flow

```
Development                Production
┌──────────┐              ┌──────────┐
│  Source  │              │  Built   │
│  Files   │              │  Assets  │
└────┬─────┘              └────▲─────┘
     │                         │
     │ Vite Build             │
     ├────────────────────────┘
     │
     ├─ TypeScript Compile
     ├─ Tailwind CSS Process
     ├─ Tree Shaking
     ├─ Code Splitting
     └─ Minification
          │
          ▼
     ┌─────────────┐
     │ frontend/   │
     │   dist/     │
     │    ├─ index.html
     │    ├─ assets/
     │    │   ├─ *.js
     │    │   └─ *.css
     │    └─ vite.svg
     └─────────────┘
          │
          ▼
     ┌─────────────┐
     │   Deploy    │
     │  (Vercel/   │
     │   Netlify)  │
     └─────────────┘
```

## 🧪 Testing Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    Testing Layers                            │
│                                                              │
│  Layer 1: Static Analysis                                   │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  • TypeScript Type Checking                           │ │
│  │  • ESLint + jsx-a11y (strict)                         │ │
│  │  • Rules enforced at build time                       │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  Layer 2: Component Testing                                 │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  • Storybook Stories                                  │ │
│  │  • Accessibility Addon                                │ │
│  │  • Visual Regression Testing                          │ │
│  │  • WCAG Compliance Checks                             │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  Layer 3: Manual Testing (Required)                         │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  • Screen Reader Testing                              │ │
│  │    - NVDA (Windows)                                   │ │
│  │    - JAWS (Windows)                                   │ │
│  │    - VoiceOver (macOS/iOS)                            │ │
│  │  • Keyboard Navigation                                │ │
│  │  • Browser Zoom (300%)                                │ │
│  │  • Touch Target Verification                          │ │
│  │  • Color Contrast Validation                          │ │
│  └────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

## 🚀 Deployment Architecture (Recommended)

```
┌───────────────────────────────────────────────────────────────┐
│                       Production                              │
│                                                               │
│  ┌─────────────────┐           ┌─────────────────┐           │
│  │   Frontend      │           │    Backend      │           │
│  │   (Vercel)      │◄─────────►│   (Railway)     │           │
│  │                 │   CORS    │                 │           │
│  │  Static Files   │           │  FastAPI + AI   │           │
│  │  CDN Cached     │           │  API Services   │           │
│  └─────────────────┘           └─────────────────┘           │
│         │                             │                       │
│         │                             │                       │
│  ┌──────▼────────────────────────────▼────────┐              │
│  │           Users (Browsers)                 │              │
│  │  • High Accessibility Requirements         │              │
│  │  • Screen Readers                          │              │
│  │  • Keyboard-Only Navigation                │              │
│  │  • Voice Control                           │              │
│  └────────────────────────────────────────────┘              │
└───────────────────────────────────────────────────────────────┘
```

---

## 📊 Tech Stack Summary

| Layer                  | Technology            | Purpose                     |
| ---------------------- | --------------------- | --------------------------- |
| **Frontend Framework** | React 18 + TypeScript | UI library with type safety |
| **Build Tool**         | Vite                  | Lightning-fast development  |
| **Routing**            | React Router v6       | SPA navigation              |
| **Head Management**    | react-helmet-async    | SEO + screen readers        |
| **Styling**            | Tailwind CSS          | Utility-first CSS           |
| **UI Primitives**      | Radix UI              | Accessible components       |
| **Linting**            | ESLint + jsx-a11y     | Accessibility enforcement   |
| **Documentation**      | Storybook             | Component library           |
| **Backend Framework**  | FastAPI               | Modern Python API           |
| **Server**             | Uvicorn               | ASGI server                 |
| **AI - Chat**          | Google Gemini         | Multimodal understanding    |
| **AI - STT**           | OpenAI Whisper        | Speech-to-text              |
| **AI - TTS**           | ElevenLabs            | Text-to-speech              |
| **Validation**         | Pydantic              | Request/response models     |

---

**Architecture designed for accessibility, scalability, and developer experience.**
